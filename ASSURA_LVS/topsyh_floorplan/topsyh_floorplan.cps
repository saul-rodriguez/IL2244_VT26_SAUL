                                                                               
; autoPinSwap() results for schematic network.

swapPins("V500_20" "")

swapPins("Current_Voltage_Reference" "(p IOUT2 IOUT3 IOUT1 IOUT4 IOUT5 IOUT6
   IOUT7)")

swapPins("V200_20" "")

swapPins("ECG_bandgap_f_v1" "")

swapPins("Constant_gm" "(p IBIAS1 IBIAS2 IBIAS3)")

swapPins("ECG_BIAS" "(p IOUT1 IOUT2 IOUT3 IOUT4 IOUT5 IOUT6 IOUT7)")

swapPins("INVX1" "")

swapPins("V1000_20" "")

; INFORMATION: pinswap analysis failed, cell will be expanded.
; Cell: chopper_f

swapPins("ia_new_f" "")

swapPins("mux21" "")

swapPins("VGA_1" "")

swapPins("cmfb_opamp1_fc" "")

swapPins("fecg_cmfb2_fc" "(p INP INN)")

swapPins("BUFFER" "")

swapPins("ECG_Front_End" "")

swapPins("fbias_gm_c1_f2c" "(p OUT1 OUT2 OUT3)")

swapPins("fbias_vol_ref1_layout" "")

swapPins("fecg_bandgap3_layout" "")

swapPins("fbias_cur_ref2_layout" "(p OUT1 OUT2 OUT3 OUT4 OUT5 OUT6 OUT7 OUT8
   OUT9 OUT10 OUT11)")

swapPins("INVX2" "")

swapPins("fbias_por2_fc" "")

swapPins("fbias_bias2_f" "(p OUT7 OUT1 OUT2 OUT3 OUT4 OUT5 OUT6 OUT8 OUT9 OUT10
   OUT11)")

swapPins("buffer2_f_3c" "")

swapPins("mux21c_f" "")

swapPins("fecg_vga2_f_2" "")

; INFORMATION: pinswap analysis failed, cell will be expanded.
; Cell: chopper2

swapPins("fecg_ia_5_fc" "")

swapPins("gmc_5_fc" "")

swapPins("fecg_front_end_f_3" "")

swapPins("GND3IP" "")

swapPins("GND3OP" "")

swapPins("GND3RP" "")

swapPins("ICP" "")

swapPins("VDD3IP" "")

swapPins("VDD3RP" "")

swapPins("PWRCUT_DIG_P_DX" "(p gndo_left! gndo_right!)")

swapPins("APRIO200P" "")

swapPins("AVDD3ALLP" "")

swapPins("AGND3ALLP" "")

swapPins("IO_RING_S_VHS" "(p (f fecg FECG) (f vinp VINP) (f vinn VINN) (f exc1
   EXC1) (f exc2 EXC2) (f cvga CVGA) (f cbuf CBUF) (f cmfb CMFB) (f cm CM) (f
   v_ec V_EC) (f we WE) (f ce CE) (f re RE) (f tia_in TIA_IN) (f pot_out POT_OUT)
   (f vpi VPI) (f vres VRES)) (p (f vinps VINPS) (f vinns VINNS) (f exc1s EXC1S)
   (f exc2s EXC2S) (f cvgas CVGAS) (f cbufs CBUFS) (f ecgs ECGS) (f cmfbs CMFBS)
   (f cms CMS) (f vress VRESS)) (p (f spi_cs SPI_CS) (f spi_mosi SPI_MOSI) (f
   spi_clk SPI_CLK) (f clk4k CLK4K) (f reset_l RESET_L))")

swapPins("NAND3X1" "")

swapPins("NOR4X1" "")

swapPins("XOR2X1" "")

swapPins("HAX3" "")

swapPins("CLKINVX4" "")

swapPins("CLKBUFX12" "")

swapPins("CLKBUFX6" "")

swapPins("BUFX16" "")

swapPins("DLY1X1" "")

swapPins("DFCX4" "")

swapPins("NOR2X3" "")

swapPins("DFCX3" "")

swapPins("BUFX8" "")

swapPins("BUFX2" "")

swapPins("CLKINVX1" "")

swapPins("NAND2X2" "")

swapPins("CLKBUFX8" "")

swapPins("CLKBUFX10" "")

swapPins("FILLCAPX32" "")

swapPins("FILLCAPX16" "")

swapPins("FILLCAPX2" "")

swapPins("FILLCAPX8" "")

swapPins("FILLCAPX4" "")

swapPins("CLKBUFX2" "")

swapPins("CLKBUFX3" "")

swapPins("CLKBUFX24" "")

swapPins("AND2X4" "")

swapPins("DFCSX2" "")

swapPins("CLKBUFX4" "")

swapPins("NOR2X6" "")

swapPins("DFCSX4" "")

swapPins("CLKBUFX16" "")

swapPins("il2244_dig" "")
