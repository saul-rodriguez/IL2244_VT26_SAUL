                                                                               
; autoPinSwap() results for schematic network.

swapPins("GND3IP" "")

swapPins("GND3OP" "")

swapPins("GND3RP" "")

swapPins("ICP" "")

swapPins("VDD3IP" "")

swapPins("VDD3RP" "")

swapPins("PWRCUT_DIG_P_DX" "(p gndo_left! gndo_right!)")

swapPins("APRIO200P" "")

swapPins("AVDD3ALLP" "")

swapPins("AGND3ALLP" "")
